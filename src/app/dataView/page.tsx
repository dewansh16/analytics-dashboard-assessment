import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <div className="flex h-dvh justify-center items-center p-10">page</div>
  );
};

export default page;
